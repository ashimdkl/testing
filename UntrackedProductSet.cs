using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UntrackedProductSet : ProductSet
{
/*    public TrackedProductsSet ConvertToTracked(int countConverted)
    {
        TrackedProductsSet trackedProductsSet = CreateInstance<TrackedProductsSet>();
        
        trackedProductsSet.Initialize(countConverted,SetNumber);
        
        trackedProductsSet.products.AddRange(products.GetRange(0,countConverted));
        
        products.RemoveRange(0, countConverted);

        return trackedProductsSet;
    }
    public TrackedProductsSet ConvertToTracked()
    {
        TrackedProductsSet trackedProductsSet = CreateInstance<TrackedProductsSet>();

        trackedProductsSet.Initialize(products.Count-1, valueRange, SetNumber);

        trackedProductsSet.products = products;

        return trackedProductsSet;
    }*/
}
