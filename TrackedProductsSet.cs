using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrackedProductsSet : ProductSet
{
/*    public float upperControlLimit;
    public float lowerControlLimit;
    public void Initialize(float upperControlLimit,float lowerControlLimit, int containerSize, Vector2 valueRange, int setNumber)
    {
        this.upperControlLimit = upperControlLimit;
        this.lowerControlLimit = lowerControlLimit;
        base.Initialize(containerSize,valueRange,setNumber);
    }
    public void Initialize(float upperControlLimit, float lowerControlLimit, ProductSet productSet)
    {
        this.upperControlLimit = upperControlLimit;
        this.lowerControlLimit = lowerControlLimit;
        base.Initialize(productSet);
    }*/
}
